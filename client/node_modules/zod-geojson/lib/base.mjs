import * as z from "zod";
import { GeoJSONBBoxGenericSchema } from "./bbox.mjs";
export const GeoJSONBaseSchema = (positionSchema) => z.object({
    bbox: GeoJSONBBoxGenericSchema(positionSchema).optional(),
});
