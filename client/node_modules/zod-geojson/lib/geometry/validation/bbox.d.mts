import * as z from "zod";
import { ValidatableCollection, ValidatableCoordinate, ValidatableGeometry, ValidatableGrid, ValidatableGridList, ValidatableList } from "./types.mjs";
export declare const getInvalidBBoxIssue: (ctx: z.core.ParsePayload) => z.core.$ZodRawIssue;
/**
 * Checks if given bbox is valid for the given position.
 * @param bbox The bbox to validate
 * @param coordinates Contains the position
 */
export declare function validBBoxForPosition({ bbox, coordinates }: ValidatableCoordinate): boolean;
/**
 * Checks if given bbox is valid for the given positions.
 * @param bbox The bbox to validate
 * @param coordinates Contains the list of positions
 */
export declare function validBBoxForPositionList({ bbox, coordinates }: ValidatableList): boolean;
/**
 * Checks if given bbox is valid for the given positions grid.
 * @param bbox The bbox to validate
 * @param coordinates Contains the grid of positions
 */
export declare function validBBoxForPositionGrid({ bbox, coordinates }: ValidatableGrid): boolean;
/**
 * Checks if given bbox is valid for the given positions grid.
 * @param bbox The bbox to validate
 * @param coordinates Contains the grid of positions
 */
export declare function validBBoxForPositionGridList({ bbox, coordinates }: ValidatableGridList): boolean;
export declare function validBBoxForCollection({ bbox, geometries }: ValidatableCollection): boolean;
export declare function getBBoxForGeometry(geometry: ValidatableGeometry): number[];
export declare function getBBoxForGeometries(geometries: ValidatableGeometry[]): number[];
export declare function bboxEquals(bbox1: number[], bbox2: number[]): boolean;
