"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validBBoxForFeature = validBBoxForFeature;
exports.validBBoxForFeatureCollection = validBBoxForFeatureCollection;
const bbox_1 = require("../geometry/validation/bbox.js");
const types_1 = require("./types.js");
function validBBoxForFeature({ bbox, geometry }) {
    if (bbox == null || geometry == null) {
        return true;
    }
    const expectedBBox = (0, bbox_1.getBBoxForGeometry)(geometry);
    return (0, bbox_1.bboxEquals)(expectedBBox, bbox);
}
function validBBoxForFeatureCollection({ features, bbox }) {
    if (!bbox) {
        return true;
    }
    const expectedBBox = (0, bbox_1.getBBoxForGeometries)((0, types_1.getGeometries)({ features }));
    return (0, bbox_1.bboxEquals)(expectedBBox, bbox);
}
