import { bboxEquals, getBBoxForGeometries, getBBoxForGeometry } from "../geometry/validation/bbox.mjs";
import { getGeometries } from "./types.mjs";
export function validBBoxForFeature({ bbox, geometry }) {
    if (bbox == null || geometry == null) {
        return true;
    }
    const expectedBBox = getBBoxForGeometry(geometry);
    return bboxEquals(expectedBBox, bbox);
}
export function validBBoxForFeatureCollection({ features, bbox }) {
    if (!bbox) {
        return true;
    }
    const expectedBBox = getBBoxForGeometries(getGeometries({ features }));
    return bboxEquals(expectedBBox, bbox);
}
